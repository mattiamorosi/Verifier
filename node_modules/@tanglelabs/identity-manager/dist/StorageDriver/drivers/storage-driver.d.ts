import { Account } from "@iota/identity-wasm/node";
import { DecryptMethod, EncryptMethod, IStorageDriverProps, StorageDriver } from "./storage-driver.types";
/**
 * Create a new storage driver
 *
 * @param {StorageDriverProps<T> } props
 * @returns {Promise<StorageDriver>}
 */
export declare function buildStorageDriver(props: IStorageDriverProps, encryptMethod: EncryptMethod, decryptMethod: DecryptMethod, account: Account): Promise<StorageDriver>;
